services:
  app:
    image: node:20-alpine
    container_name: neon-proxy-app
    command: sh -c "node -e \"require('http').createServer((_,r)=>r.end('neon proxy placeholder')).listen(7500)\""
    ports:
      - "7500:7500"
